commit-msg:
  commands:
    lint:
      run: npx lint-commit-message {1}
pre-commit:
  parallel: true
  commands:
    test:
      run: npm run test
    build:
      run: npm run --silent build && git add README.md
